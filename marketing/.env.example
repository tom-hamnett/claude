# Stripe payment links — drop in once Stripe is configured.
# These are Stripe Payment Links (https://dashboard.stripe.com/payment-links),
# not full Checkout API URLs. The unlock page falls back to /sigma/checkout-soon
# placeholder if these are not set.
VITE_STRIPE_LINK_BYOK=https://buy.stripe.com/your-byok-link
VITE_STRIPE_LINK_MANAGED=https://buy.stripe.com/your-managed-link
