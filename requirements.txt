# Core AI
anthropic>=0.40.0
google-genai>=1.0.0
pydantic>=2.9.0
python-dotenv>=1.0.0

# Web UI
streamlit>=1.39.0

# Utilities
httpx>=0.27.0
tenacity>=9.0.0
rich>=13.9.0
Pillow>=10.0.0
imageio-ffmpeg>=0.5.1   # bundled ffmpeg binary — extract audio from uploaded video
openpyxl>=3.1.0         # read uploaded .xlsx spreadsheets for the data-analysis step
pypdf>=4.0.0            # read uploaded PDFs (CVs, reports) in the content intake
python-docx>=1.1.0     # read uploaded Word docs
python-pptx>=0.6.23    # read uploaded PowerPoint decks

# These are only needed for CLI deployment features (not the UI):
# praw>=7.7.0          # Reddit
# tweepy>=4.14.0       # X/Twitter
# sendgrid>=6.11.0     # Email
# APScheduler>=3.10.0  # Scheduling
